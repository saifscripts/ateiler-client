import { ProductList, ProductListHeader } from '../../features/products';

const Products = () => {
  return (
    <>
      <ProductListHeader />
      <ProductList />
    </>
  );
};

export default Products;
