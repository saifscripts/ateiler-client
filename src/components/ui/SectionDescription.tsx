const SectionDescription = ({ children }: { children: React.ReactNode }) => {
  return (
    <p className="text-body-2 text-center mb-8 text-metal-500 max-w-xl mx-auto px-4">
      {children}
    </p>
  );
};

export default SectionDescription;
